<UserControl x:Class="BookLibrary.View.Shelf"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="405">

    <ListView x:Name="BookListView" Grid.Row="5" ItemsSource="{Binding Path=BookListCollection}" HorizontalAlignment="Left" Width="405" HorizontalContentAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderBrush="Black" Grid.ColumnSpan="2" Grid.RowSpan="4" Height="300" VerticalAlignment="Bottom" Margin="0,0,-111,0" >
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Обложка" DisplayMemberBinding="{Binding Cover}" Width="150" />
                <GridViewColumn Header="Описание" DisplayMemberBinding="{Binding Description}" Width="100"/>
                <GridViewColumn Header="Автор" DisplayMemberBinding="{Binding Author}" Width="100"/>
                <GridViewColumn Header="Год" DisplayMemberBinding="{Binding Year}" Width="50"/>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
